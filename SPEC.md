# 聲擂 SoundArena — 產品規格文件

音樂比賽投票網站,支援 AI 音樂平台(以 Suno 為主)創作者投稿、多輪賽制淘汰、匿名投票,並透過 LINE bot / Discord bot 發送賽事通知。

最初的畫面原型:https://preview--sound-stage-showdown.lovable.app/(Lovable + Supabase Auth,寫這份規格時只有前端殼與空狀態頁面)——這份規格目前已大部分實作完成,現況以 [HANDOFF.md](HANDOFF.md) 與 [docs/adr/](docs/adr/) 為準,規格本身記錄的是設計意圖(WHAT/WHY),實作細節可能因後續資安複查/使用者需求而調整,調整都會補一篇 ADR。

版本:2026-08-09 初版,依 grill-me 訪談定案;2026-08-11 因競品調研(songcontest.ai)修訂為開放多租戶平台,見 ADR-0002

---

## 0. 平台定位

- SoundArena 是**開放多租戶平台**,不是站方自己主辦比賽的單一網站(決策理由見 `docs/adr/0002-open-multi-tenant-platform.md`)
- 任何使用者都可以送出「成為 Organizer」的申請,但要等 **PlatformAdmin 審核通過**(`host_approved_at` 不為 null)才能建立/管理 Competition——ADR-0002 當初的「自動成為 Organizer、不需審核」已在 ADR-0014 反轉,現行規則以 ADR-0014 為準
- 權限分三層:
  - **Organizer**:管理自己建立的 Competition(審核投稿、賽制建立、時程設定),看不到其他人的比賽後台
  - **Collaborator**:Organizer 可以把單場比賽的部分管理權限(審核投稿、賽制建立、時程設定、評分、邀請其他協作者五種,可個別勾選)委派給其他使用者,範圍僅限被邀請的那一場比賽(見 `docs/adr/0003-collaborator-tiered-permissions.md`)
  - **PlatformAdmin**:看得到全站所有 Competition,負責 Organizer 申請的審核/撤權,以及草稿期以外比賽的刪除;不是自助開放角色,由平台方手動指派
- **Report(檢舉)機制已移除**:曾經規劃過「使用者可對整場比賽檢舉,進入 PlatformAdmin 處理清單」,但已在 `docs/adr/0007-remove-report-mechanism.md` 拿掉,目前系統沒有任何檢舉功能;投稿內容的把關仍由該比賽自己的 Organizer/review 協作者在審核流程處理(見第 3 節)

---

## 1. 帳號與登入

- 登入選項並列三種:Google、LINE Login、Discord OAuth
- 使用 LINE Login 登入時,登入流程中自動跳出「加入 LINE 官方帳號好友」提示,加入後才能透過 Messaging API 接收推播通知
- 使用 Discord 登入時,採用 OAuth2 `guilds.join` 授權範圍,使用者同意後自動加入 SoundArena 的 Discord 伺服器(bot 需已常駐該伺服器);此授權範圍在部分情境下也能讓 bot 直接私訊使用者
- 登入頁面須說明清楚為什麼要加好友/連結 bot,以及 bot 能做什麼(例如可透過指令如 `/狀態` 查詢投稿與投票進度)
- 登入頁說明欄需明確告知:若不想收到任何通知,選擇 Google 登入即可——Google 登入不會觸發 LINE 加好友或 Discord `guilds.join` 流程,是唯一不會被拉進任何通知管道的登入方式
- Discord 是否額外採用「共用伺服器頻道」發送公開賽事公告(除了私訊之外),留待後續決定,不影響目前架構

### LINE / Discord / Google 技術備忘

**Google**
- 用 Supabase 內建的 `google` provider,Web Application 類型,Redirect URI 填 Supabase callback(`https://xmcesnfcrqzjuystfcil.supabase.co/auth/v1/callback`)
- 請求 scope:`openid` + `userinfo.email` + `userinfo.profile`

**Discord**
- 用 Supabase 內建的 `discord` provider
- 登入請求需帶 `guilds.join` scope(疊加在原本的 `identify` 之上);屬敏感授權範圍(過去曾被惡意 App 濫用把人拉進陌生伺服器),Discord 會顯示明確同意畫面,不是使用者無感的全自動流程,體感上與 LINE「跳出加好友提示」類似
- 「把使用者加入 SoundArena Guild」這個動作,實際上是呼叫 Discord API `PUT /guilds/{guild_id}/members/{user_id}`,需要:
  - Header 帶 Bot Token(`Authorization: Bot <BOT_TOKEN>`)
  - Body 帶使用者的 OAuth access token(`access_token` 欄位)——這個值來自登入成功當下 Supabase session 裡的 `provider_token`
  - **`provider_token` 只在登入完成當下的那個 session 物件裡出現一次,Supabase 不會把它存進資料庫**,必須在前端登入完成的瞬間立刻把它送到後端 API route 使用,過了那個時機點就再也拿不到,不能事後補查——這是實作時最容易漏掉的坑
  - 這支 `PUT /guilds/.../members/...` 呼叫**必須在後端執行**;Bot Token 絕對不能出現在前端程式碼或任何瀏覽器可讀到的地方
- 前提仍是:Bot 要先被邀請進 SoundArena 自己的 Discord 伺服器,才能執行上述 API 呼叫

**LINE**
- Supabase **沒有**內建的 LINE provider,不要誤植成跟 Google/Discord 一樣的下拉選項設定方式
- 走 Supabase 的 Custom OAuth/OIDC Provider 機制,Issuer URL 填 `https://access.line.me`
- provider identifier 用 `custom:line`(呼叫時 `signInWithOAuth({ provider: 'custom:line' })`)
- email 視為 optional,暫不要求 email scope,不影響登入流程能不能完成

**通知對象識別(Notification Identity)**
- 所有通知的收件對象一律以 Supabase `auth.users.id`(= `profiles.id`)為主鍵查找,這是唯一的身份識別基準
- Email(`auth.users.email`,只有 Google 登入會自然帶入)、Discord User ID(`profiles.discord_user_id`)、LINE User ID(`profiles.line_user_id`)都只是「通知送到哪裡」的目的地欄位,不是身份識別的主鍵——同一個人不會因為換了通知管道就變成不同使用者
- LINE Login(登入)與 LINE Messaging API(推播)是兩個獨立系統,須串接才能兩者兼具

---

## 2. 報名與投稿

### 存取順序(硬性)
- 未登入(見第 1 節)不得進入報名頁,只能瀏覽活動/比賽的公開資訊
- 未完成報名不得進入投稿頁——投稿頁需先確認該帳號在該賽事已有報名紀錄,否則導回報名頁
- 順序固定為:登入 → 報名 → 投稿,不可跳過前一步直接進下一步

### 報名
- 填寫站內帳號(見上)
- 填寫 Suno 帳號名稱或個人主頁網址(用於後續身份比對)

### 投稿(每輪皆須重新投稿)
- 選擇本次投稿所屬的賽制/比賽場次
- 貼上 Suno 作品分享連結
- 上傳音檔案(播放用,見第 4 節)
- 手動貼上歌詞(無公開 API 可自動取得,見附錄調查結果)
- 審核通過後,系統自動把該投稿加入所選賽制對應輪次的歌曲清單,不需人工另外搬移

### 投稿資格與時間邊界
- 報名、投稿、投票三個階段的時間窗須互相檢查邊界,不可讓「投稿期已結束」之後還允許新報名進來搭上同一輪
- 投稿資格與晉級狀態掛鉤:於某輪遭淘汰的參賽者,後續輪次僅保留投票權限,不得再投稿
- 淘汰制賽事可設定「僅開放特定輪次投稿」(例如僅初賽、複賽開放新投稿,決賽只留晉級者的既有作品比拼),由主辦於賽制/時程設定頁自訂哪幾輪開放投稿

### 連結解析與自動帶入
貼上 Suno 分享連結後,**由後端伺服器**呼叫公開端點取得以下資料並自動帶入投稿表單:
- 歌曲標題、封面圖(來自頁面 Open Graph meta 標籤)
- 作者顯示名稱與帳號 handle(來自 `studio-api-prod.suno.com/api/share/code/{分享碼}`,確認為公開端點,免登入可用)

**硬性限制:這支呼叫必須在後端做,前端瀏覽器不能直接呼叫**——實測從第三方網頁(含 `file://` 本機檔案)對這支端點發 fetch,會被 CORS 擋下(`No 'Access-Control-Allow-Origin' header`)。「免登入」只代表伺服器對伺服器呼叫不需要驗證身份,不代表瀏覽器可以跨網域直接打;現有 huashu-design 原型(純前端無後端)因此**無法**對任意連結做到即時真實解析,只能用固定的 mock 對照表示範流程,這是原型架構的硬限制,不是可以在前端修好的 bug。

歌詞、完整音檔串流網址等資料**沒有找到公開/免登入的取得管道**,不能自動抓取,只能由投稿者自行提供。

---

## 3. 身份驗證(同一人檢查)

- 投稿連結解析出的 `sharer_handle` 與報名時填寫的 Suno 帳號名稱比對
- 比對通過 → 自動放行,進入人工審核清單
- 比對不通過 → 擋下,要求投稿者更正,或由該賽事的 Organizer 人工放行

已用兩組真實案例驗證 `sharer_handle` 對應的是作品作者本人(非單純分享者),可信賴此欄位做身份比對。

### 「不公開」規則的檢查方式
- 比賽規則若要求投稿作品需設為 Suno 上的「不公開」狀態,由人工審核時打開作者公開主頁(`suno.com/@handle`),確認該首歌未列在公開作品清單中
- 已驗證此方法可靠:對「不公開」作品進行完全匿名(無登入態)請求,主頁 HTML 中確實搜尋不到該作品標題

---

## 4. 播放架構

### 音源
- **不採用 Suno 官方 iframe 嵌入**,改為要求投稿者上傳音檔案,存放於私有雲端儲存(不可公開列目錄)
- 後端依請求動態簽發**短效期網址**,供播放器讀取;現行實作預設 1 小時過期(見 `web/src/lib/storage.ts` 的 `getPlaybackUrl()`),簽發前需確認請求者有權限看到這個 `audio_object_key`(RLS 判斷式見 `web/src/lib/playbackActions.ts`)
- 原因:
  1. 呼應最初「連結不可被任意轉發外流」的需求——Suno CDN 上找得到未加簽章、永久有效、免登入即可下載的直連音檔網址(`cdn1.suno.ai/{歌曲UUID}.mp3`),不適合作為正式播放來源
  2. 唯有自己掌握真正的 `<audio>` 元素,才能串接 Media Session API 做到鎖屏播放控制(見下)
- 明確不採用 EME/DRM:與此專案的實際威脅模型(防止使用者隨手複製連結轉發)不成比例,建置與維運成本過高,且 Apple FairPlay 需另外向 Apple 申請認證

### 背景/鎖屏播放
- 接 Media Session API,Android Chrome、iOS Safari 皆有支援
- 效果:分頁背景執行、切換 App、鎖屏時聲音持續播放,且鎖屏/通知列顯示播放控制列(歌名、暫停、切歌)
- 提醒:不同手機/瀏覽器背景分頁行為本就不完全一致(尤其 iOS Safari 較嚴格),無法保證所有裝置 100% 一致

### 防止多首同時播放
- 全站僅維護「單一播放器」實例,歌曲清單本身只是可點擊列表(非各自獨立的嵌入播放器)
- 換歌時,銷毀舊音源、載入新音源,不使用第三方 iframe 控制 API(因為 Suno 未提供)
- 賽制若需要並排比較兩首(對戰模式),UI 上呈現兩個清單項目,但實際播放仍收斂到同一個播放器實例,不會同時發聲

### 頁面結構
- 單一播放器 + 依「第 N 輪」分組的歌曲清單(可視為歌單/queue),點選清單項目載入播放器
- 播放器固定於版面中不隨頁面路由重新掛載,以支援站內切頁時繼續播放

---

## 5. 匿名投票與防灌票

- 是否匿名、每輪是否重新匿名等規則,於「建立比賽」時可自由設定(見第 8 節賽制系統),非寫死的單一模式
- 後台(審核角色)永遠知道真實身份,用於身份驗證與內容審核;前台是否對投票者揭露依比賽規則決定

### 匿名揭露時機(主辦可選其一)
| 模式 | 說明 |
|---|---|
| 全程匿名,決賽才公開 | 各輪投票期間皆匿名,直到決賽結束才一次公開全部參賽者身份 |
| 單輪匿名,賽後公開 | 每一輪各自匿名,該輪投票一結束就公開該輪作者身份,不等到決賽 |
| 全程公開 | 從頭到尾不匿名,投票時就看得到作者 |

上表三種模式共用同一套匿名/隨機排序機制(見下),差別只在「公開的時間點」,不是三套獨立系統。
- 投稿清單順序:**匿名投票階段**,每次讀取清單皆重新隨機排序(同一使用者重複刷新也會拿到不同順序),避免固定位置被鎖定灌票;**比賽進入公開階段後不隨機**,依正常規則排序顯示
- 投票紀錄綁定投稿在資料庫中的固定內部編號,不綁定播放短效網址或清單顯示位置,確保清單隨機化、播放網址過期輪替都不影響事後對票的正確性
- 不能投給自己的作品
- 同一 IP 更換帳號重複投票直接限制(已知會有同校/同公司共用 IP 的正常使用者被誤傷的可能性,此為主辦方接受的取捨)

### 角色權限分離
- 「身份驗證/合規審核」與「內容評分」是兩個獨立的後台角色/權限
- 評審不應看到投稿者真實身份,避免因認出熟悉的作品而產生不必要的評分偏袒
- 這兩個角色都是**該場 Competition 的 Organizer 底下**的權限分工(可委派給 Collaborator),範圍僅限該場比賽;跨比賽的權限(看全站、審核 Organizer 申請)是 PlatformAdmin,兩者不是同一層級(見第 0 節)

### 公開試聽(Discovery 頁用)
- 投稿時 Participant 可自選是否「允許公開展示」該作品,預設關閉
- 只有標記允許的作品,才會在未登入的 Discovery 頁(見第 10 節)出現可試聽的公開播放網址;未標記的作品維持原本「需登入才能簽發播放網址」的規則
- 公開試聽網址與一般播放網址是分開的簽發規則,不是把一般短效網址直接曝光給未登入使用者

---

## 6. 通知系統

### 設計原則
每一則通知都要精準對應到特定使用者(透過 registration/user 的內部 UID 查找收件人),不是廣播式公告——例如「該輪淘汰結果」要分別通知被淘汰者與晉級者各自不同的內容,不是同一則訊息群發。

### 訂閱範圍(誰會收到通知,鐵律)
- **建立/主辦一場比賽不會讓 Organizer 訂閱通知**——單純建立比賽不代表想收到那場比賽的所有動態,不能因為辦了比賽就被通知轟炸
- **報名(Registration)才是訂閱動作**——完成報名的當下才成為該場比賽的通知訂閱者,這是唯一的觸發點,不是登入、不是瀏覽、不是建立比賽
- 訂閱可取消——已報名者可以主動取消追蹤該場比賽的通知,不強制綁定
- 通知管道(Email/Discord/LINE)由登入方式決定收不收得到(見下),但「要不要收」這件事本身,範圍還是綁在「報名了哪些比賽」,不是全站廣播

### 觸發時機(涵蓋整場比賽生命週期)
- 報名成功
- 已報名但超過主辦設定的時間段仍未投稿(nag reminder;時間段由主辦自訂,不是系統寫死的固定天數)
- 投稿完成(送出成功的確認通知)
- 該輪投票開始(提醒可投票)
- 晉級到下一輪、且該輪開放新投稿時,提醒晉級者投稿(呼應第2節「僅開放特定輪次投稿」)
- 該輪淘汰/晉級結果:於該輪投票截止、排名計算完成後發送,發送前需經人工審核確認(作為結果公布前的調整緩衝,例如發現異常可暫緩);被淘汰者與晉級者分開通知
- 最終名次/冠軍公布(整場比賽結束)

### 管道
- LINE(Messaging API 推播)、Discord(私訊或伺服器頻道,待定)、**Email**(需求已記錄,寄信服務尚未選定/接線,見下方待辦)
- Bot 需支援基本查詢指令(如 `/狀態`),讓使用者可主動詢問投稿/投票進度,非純粹的單向推播

### Email 管道(待辦,尚未實作)
- 只有用 **Google 帳號登入**的使用者會收到 mail 通知——LINE/Discord 登入不額外收集 email,不提供 mail 選項,以登入方式本身決定,不是另外的可勾選設定
- 尚未決定寄信服務商(例如 Resend、SendGrid),先記錄需求,實作與廠商選型留待後續

---

## 7. 賽制系統

> 詞彙定義見 CONTEXT.md。這裡只寫規則,不重複定義 Competition / Round / FormatBlock / FormatTemplate。

- **FormatBlock 掛在 Round 上,不是掛在 Competition 上**(見 ADR-0001):同一場 Competition 底下,不同 Round 可以是完全不同的賽制組合——例如第 1 輪循環賽、第 2 輪 3 對 3 隊伍賽、決賽單挑對戰,不強制整場統一
- FormatBlock 三類:
  - **淘汰方式**:單敗淘汰、雙敗淘汰(敗部復活)、循環賽、月度/週期累積制
  - **分組方式**:個人賽、隊伍賽(如 3 對 3,可用團隊加總或平均計分)、抽籤分組
  - **特殊機制**:敗部復活戰(wildcard)、限定主題輪(mission round)、業界導師制
- **固定骨架**:不論每輪 FormatBlock 怎麼組合,Competition 的第一個 Round 固定是「初賽」、最後一個固定是「決賽」,中間 Round 數量與組合自訂
- FormatTemplate 範本庫走**豐富預建**路線:盡量把常見組合先做成可一鍵套用到 Round 上的範本,不要求主辦每次都要從零組裝;主辦仍可在範本之外自訂 Round 的 FormatBlock 組合,新邏輯有需要再擴充開發

### 限定主題輪的主題類型
- 主題可以是文字題(關鍵字/詞句限定,見第 8 節「關鍵字/主題符合加分」)或**曲風限定**
- 曲風限定的合規檢查,資料來源**待驗證**,有兩條可能路徑:
  1. 從投稿連結解析曲風標籤 — 與歌詞相同,目前找到的 Suno 公開端點都沒有回傳曲風欄位,大機率會撞到跟歌詞一樣的限制(僅登入本人帳號才看得到),需要實測確認,不能預設可行
  2. 從投稿者上傳的音檔案本身分析 — 檢查匯出的 MP3 檔案 ID3 標籤是否帶有曲風資訊,或串接音訊分類服務辨識曲風;這條路徑理論上可行,但兩個子選項都還沒拿到實際 Suno 匯出檔案測試過
- 在其中一條路徑實測驗證可行之前,曲風合規檢查先假設走**人工審核判斷**,不承諾自動化

---

## 8. 評分機制

- ScoringRule 存在兩個層級:**Competition 設一個預設值,套用到所有未特別設定的 Round**;個別 Round 需要不同計分項目時(例如限定主題輪要啟用「關鍵字符合加分」),可用 ScoringRuleOverride 取代該輪的預設值——多數比賽全程用同一套即可,不用每輪重新設定
- 評分規則不是寫死的固定維度,而是主辦方從一組**可用輸入項目(ScoreItem)**中挑選要啟用哪些、並各自設定權重,組成專屬的計分公式
- 硬性要求:**完整計算公式必須公開**,參賽者與投票者都能看到分數是怎麼算出來的,不做黑箱計算(呼應第 7 節賽制系統的積木化精神)

### 可用輸入項目(範本庫,可擴充)
| 項目 | 資料來源 | 性質 |
|---|---|---|
| 投票 | 站內投票結果 | 系統自動計算 |
| 外部投票 | 站外平台(如社群投票)匯入的票數 | 需主辦手動或排程匯入,非站內產生 |
| 影片流量 | YouTube 觀看數等(見下方細節) | 系統排程抓取,自動計算 |
| 關鍵字/主題符合加分 | 比對投稿歌詞是否符合該輪限定主題/關鍵字(呼應第 7 節「限定主題輪」) | 系統文字比對,可能仍需人工確認邊界案例 |
| 魔王加給 | 特定評審(如業界導師/嘉賓評審)手動給予的額外加分 | 人工輸入,唯一需要評審介面手動操作的項目 |

- 上述項目中,多數是**系統自動計算的唯讀數據**;評審實際要操作的介面,主要是「魔王加給」這類需要人工判斷的加分項,其餘項目在評審畫面上以唯讀明細呈現,不是每次評分都要重新手動填寫
- 每個項目的權重由主辦在建立比賽時設定,同一場比賽可以只啟用其中一兩項,不用全部項目都用上
- 輸入項目庫同樣走豐富預建路線(呼應第 7 節),新類型有需要再擴充

### 權重規則(硬性)
- 計入排名的項目,**權重總和必須等於 100%**,由系統在主辦設定時即時檢查、不合就擋下不給儲存
- 「額外加分」類型的項目(如魔王加給)可以在建立比賽時標記為**不計入 100% 封頂**的加分項——這類項目是在已經算好的加權小計之上另外累加,不受權重總和限制,主辦可自行決定哪些項目屬於這一類
- 換句話說:一場比賽的分數 = (權重總和 100% 的核心項目算出的加權小計) + (任意數量、任意分值的額外加分項目)

### 影片流量加分(細節待展開)
- YouTube 觀看數加分為**可選輸入項目**,建立比賽時自由決定是否啟用
- 加分換算公式、資料更新頻率、YouTube API 額度配置等實作細節尚未展開

---

## 9. 時程與階段

- 標準階段骨架:宣傳 → 投稿 → 投票 → 公布(可能包含初賽/複賽/決賽等多輪)
- 各階段起訖日期由主辦方於後台自訂,系統不寫死固定天數
- 階段時間窗須互相檢查邊界(見第 2 節「投稿資格與時間邊界」),例如報名開放時間不可晚於投稿期結束,避免投稿期已過還能報名進場
- 業界參考節奏(供主辦排程時參考,非強制):報名截止到公布下一階段名單,常見抓 1–2 週緩衝

---

## 10. UI 設計方向(第二輪迭代已定案)

- 播放控制列採玻璃擬態(glassmorphism)風格,懸浮於內容之上、不完全遮蔽封面
- 整體走低刺激設計:深色模式為主、留白充足、清楚但不誇張的微互動回饋
- Icon 統一加入輕量動畫(hover/狀態切換過渡),但不做裝飾性動畫堆疊
- AI 個人化推薦(如 Spotify DJ 那類功能)不適用於本專案,不需納入設計範圍

### 導覽位置
- 公開頁面(登入、報名、投稿、歌曲清單、投票)採**頂部 header 導覽**,符合內容瀏覽型網站(而非長時間停留的工具型 App)的慣例,比照 Toornament/Devpost 這類賽事網站,而非 Spotify 式常駐左側導覽
- 管理/儀表板類頁面(審核後台、賽制建立、時程設定)採**左側可收合側欄**,符合多分區後台管理介面的慣例(如 Stripe/Notion 後台)
- 兩種導覽服務不同頁面類型,不用統一成同一種

### 歌曲清單呈現
- 播放器歌曲清單改採可摺疊展開的分組呈現(比照 YouTube 播放清單的輪次分組摺疊),而非全部展開的長列表
- 每首歌曲項目需提供連結,可點回其所屬的比賽活動頁面

### 邊界狀態設計
- 每個列表類畫面(投稿清單、投票清單、審核清單、評分佇列等)都需要明確設計「空狀態」(0 筆資料時的畫面),不可放任空白區塊或讓畫面看起來像尚未載入完成
- 錯誤/失敗狀態(如連結解析失敗、身份比對不通過)需明確呈現具體原因,不可靜默不顯示任何回饋

### Discovery(比賽發現頁,取代原本的首頁佔位)
- 首頁/「活動」頁改為 Competition 瀏覽頁,呈現全平台(所有 Organizer)建立的比賽,支援基本篩選(狀態:進行中/即將開始/已結束;可能再加分類)
- 每張 Competition 卡片顯示:名稱、Organizer、目前階段狀態、參賽/投稿數量等基本資訊
- 允許公開展示的作品(見第 5 節)可在此頁直接試聽,不需登入
- 空狀態(篩選後無結果)需給出清楚的重置篩選路徑,不可只顯示空白

### 管理後台的兩種視角
- 左側可收合側欄(見上「導覽位置」)在多租戶下要能反映使用者的角色範圍:Organizer 登入後只看到自己建立的 Competition 清單與其審核後台/賽制建立/時程設定;PlatformAdmin 另有獨立視角看全站 Competition、Organizer 申請審核清單
- 兩種視角不共用同一份清單資料,避免 Organizer 誤以為自己能看到別人的比賽

---

## 附錄:技術調查紀錄

以下為訪談過程中實測驗證的技術事實,供日後開發參考。

### Suno 公開資料端點
- `GET https://studio-api-prod.suno.com/api/share/code/{分享碼}`:公開、免登入,回傳 `content_id`、`content_type`、`sharer_display_name`、`sharer_handle`、`sharer_avatar_url`
- `https://suno.com/@{handle}`:公開個人主頁,免登入可存取,列出該帳號的公開作品
- `https://suno.com/embed/{歌曲ID}`:官方嵌入播放器 widget(本專案最終決定不採用,原因見第 4 節)
- `https://cdn1.suno.ai/{歌曲UUID}.mp3`:未加簽章、永久有效的直連音檔網址(本專案不採用此作為播放來源,原因見第 4 節)
- Suno 官方**沒有公開 Developer API / OAuth**(2026 年 7 月官方僅表示「正在探索」,僅限少數合作夥伴,無文件、無時程),因此無法做「投稿者用 Suno 帳號登入授權」這類串接

### 已排除的技術方案與原因
- **投稿者 Suno OAuth 授權**:Suno 未開放公開 API,官方管道不存在
- **直接嵌入 Suno iframe 播放**:無法做到鎖屏 Media Session 控制;Suno 未提供 postMessage 控制 API,多首並存時無法防止同時播放;嵌入頁面本身有 Cloudflare Turnstile 防爬蟲機制
- **自動爬取歌詞**:公開頁面/API 皆無歌詞欄位,僅登入本人帳號才看得到,判定為資料本身不存在於公開介面,非技術難度問題
- **EME/DRM 音檔保護**:與實際威脅模型不成比例,建置與跨平台(Widevine/FairPlay 需分開建置)維運成本過高
